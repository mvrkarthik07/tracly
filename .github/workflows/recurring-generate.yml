name: Recurring Transactions

on:
  schedule:
    - cron: '5 16 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: 'curl -f -X POST https://finance-tracker-api.onrender.com/api/recurring/generate -H "X-Cron-Secret: ${{ secrets.CRON_SECRET }}"'
