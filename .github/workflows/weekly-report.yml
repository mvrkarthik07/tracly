name: Weekly Report

on:
  schedule:
    - cron: '1 16 * * 0'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: 'curl -f -X POST https://finance-tracker-api.onrender.com/api/reports/generate -H "X-Cron-Secret: ${{ secrets.CRON_SECRET }}"'
